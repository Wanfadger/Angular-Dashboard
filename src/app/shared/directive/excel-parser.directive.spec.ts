import { ExcelParserDirective } from './excel-parser.directive';

describe('ExcelParserDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcelParserDirective();
    expect(directive).toBeTruthy();
  });
});
